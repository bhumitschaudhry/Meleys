ci: Add comprehensive CI/CD pipeline with 157 unit tests

- GitHub Actions workflow (.github/workflows/ci.yml) with 9 jobs:
  - Rustfmt format check
  - Clippy lint analysis
  - Multi-platform build matrix (Linux, macOS, Windows)
  - Unit test suite across all platforms
  - MSRV verification (Rust 1.75.0)
  - Security audit via cargo-audit
  - Unused dependency detection via cargo-machete
  - Cross-compilation checks (musl, aarch64)
  - Code coverage with cargo-llvm-cov + Codecov upload
  - Release build verification

- 157 unit tests across 8 modules:
  - config (12 tests): defaults, serialization, TOML parsing, JSON roundtrip
  - error (14 tests): all error codes, retryability, type conversions
  - observation (16 tests): construction, serialization, all ActionResult variants
  - selector (21 tests): JS expressions, descriptions, JSON roundtrips, all variants
  - search (32 tests): DDG/Bing/Google adapters, URL encoding, registry management
  - profile (10 tests): sanitization, path expansion, directory creation
  - interaction (8 tests): find expression building for all selector types
  - transport/http (17 tests): router creation, health endpoint, session management,
    error handling for nonexistent sessions, search engine configuration

- Fixed axum 0.8 route syntax (changed :param to {param} capture groups)

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
